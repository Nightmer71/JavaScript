/* 

Math:
    Built-in object that provides a collection of properties and methods

*/

let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x); // Rounds to a whole number
// z = Math.floor(x); // Rounds down to a whole number
// z = Math.ceil(x); // Round up to a whole number
// z = Math.trunc(x); // Levágja a tizedes pont mögötti számokat
// z = Math.pow(x, y); // Raises x to the power of y
// z = Math.sqrt(x); // Square root of x
// z = Math.log(x); // x logaritmikus formája
// z = Math.sin(x); // sin
// z = Math.cos(x) // cosin
// z = Math.abs(x) // absolute value of x

// let max = Math.max(x, y, z); // Chooses the maximum value out of the given variables
// let min = Math.min(x, y, z); // Chooses the minimum value out of the given variables

console.log(z);